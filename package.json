{
  "name": "omp-codex-lb-responses",
  "version": "0.19.1",
  "type": "module",
  "private": false,
  "description": "OMP plugin: a clean codex-lb provider that routes the OpenAI Responses API over codex-lb's WebSocket transport — no source patcher, no global monkeypatch, no synthetic JWT.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/YanzuoLu/omp-codex-lb-responses.git"
  },
  "omp": {
    "name": "Codex LB Responses",
    "description": "Registers a codex-lb provider that streams OpenAI Responses over codex-lb's WebSocket. Configure with `omp plugin config set omp-codex-lb-responses baseUrl <your codex-lb /v1 endpoint>` and `… apiKey <sk-clb-…>` (or the CODEX_LB_* env vars).",
    "extensions": ["src/index.ts"],
    "settings": {
      "baseUrl": {
        "type": "string",
        "description": "Your codex-lb /v1 endpoint. The plugin opens wss://…/responses derived from it.",
        "env": "CODEX_LB_BASE_URL"
      },
      "apiKey": {
        "type": "string",
        "secret": true,
        "description": "codex-lb API key (sk-clb-…), sent as a plain Authorization: Bearer.",
        "env": "CODEX_LB_API_KEY"
      },
      "providerId": {
        "type": "string",
        "description": "Provider id shown in the model picker (<id>/<model>).",
        "default": "codex-lb",
        "env": "CODEX_LB_PROVIDER_ID"
      },
      "models": {
        "type": "string",
        "description": "Comma-separated model ids to register instead of the built-in catalog.",
        "env": "CODEX_LB_MODELS"
      },
      "webSearch": {
        "type": "string",
        "description": "Web search via codex-lb: 'card' = a native-identical codex web_search (overrides omp's built-in web_search for codex-lb turns; full Search card + sources); 'inject' = hosted web_search tool injected per turn (no card). Default off.",
        "env": "CODEX_LB_WEB_SEARCH"
      },
      "searchModel": {
        "type": "string",
        "description": "Model used for `webSearch: card` search requests (defaults to your first model, e.g. gpt-5.5).",
        "env": "CODEX_LB_WEB_SEARCH_MODEL"
      }
    }
  },
  "scripts": {
    "test": "bun test"
  },
  "dependencies": {
    "@oh-my-pi/pi-ai": "^16.1.19",
    "@oh-my-pi/pi-catalog": "^16.1.19"
  },
  "keywords": [
    "omp",
    "oh-my-pi",
    "codex",
    "codex-lb",
    "websocket",
    "responses"
  ]
}
